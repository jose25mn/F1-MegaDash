1 +# F1 MegaDash

      +Para rodar o projeto localmente:
      
     1. Instale Node.js (inclui npm) e certifique-se de que o ambiente consegue instalar projetos React.
     6 2. Na raiz do projeto, rode:
     7 +   ```bash
     8 +   npm install
     9 +   ```
    10 +   Isso instalará o React e todas as demais dependências listadas no `package.json`.
    11 +3. Após a instalação, suba o app (exemplo):
    12 +   ```bash
    13 +   npm run dev
    14 +   ```
    15 +   Use o script que preferir (`npm run dev:client` para o front, `npm run dev` para o servidor).
    16 +
    17 +Certifique-se de ter o banco configurado conforme as variáveis de ambiente de conexão PostgreSQL utilizadas no
         projeto.
